# 抖音视频解析工具 - Python 依赖

# 核心
requests>=2.31.0
python-dotenv>=1.0.0

# a_bogus 签名算法所需
gmssl>=3.2.0

# 本地语音转录（可选 - 使用 --no-transcript 跳过）
faster-whisper>=1.0.0

# Web 服务
flask>=3.0.0
gunicorn>=21.0.0

# 数据存储
SQLAlchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.1.0
